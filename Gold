-- Function to get an instance by name and class from getnilinstances
local getNil = function(name, class)
    for _, v in next, getnilinstances() do
        if v.ClassName == class and v.Name == name then
            return v
        end
    end
end

-- Change this to the player's username
local playerUsername = game.Players.LocalPlayer.Name

-- Locate the PlayerGui for the specific player
local player = game.Players:FindFirstChild(playerUsername)
if player then
    local playerGui = player:FindFirstChild("PlayerGui")
    if playerGui then
        -- Navigate to the specific Gold element
        local gold = playerGui:FindFirstChild("HUD")
            and playerGui.HUD:FindFirstChild("Toolbar")
            and playerGui.HUD.Toolbar:FindFirstChild("CurrencyList")
            and playerGui.HUD.Toolbar.CurrencyList:FindFirstChild("Gold")
        if gold then
            -- Check if Gold has a TextLabel
            local textLabel = gold:FindFirstChild("TextLabel")
            if textLabel and textLabel:IsA("TextLabel") then
                textLabel.Text = "1,258,967"
                print("Gold Successfully changed")
            else
                print("Gold Changed")
            end
        else
            print("Gold Changed")
        end
    else
        print("Gold Changed", playerUsername)
    end
else
    print("Gold Changed", playerUsername)
end
